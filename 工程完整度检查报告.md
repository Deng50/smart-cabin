# 智慧座舱测评体系工程完整度检查报告

## 1. 检查范围

本报告以 design_one.md 的目标、验收标准和实施顺序为基线，检查当前仓库的代码、配置、演示数据、测试、CLI、FastAPI 和 Docker 配置。

检查日期：2026-09-11  
检查方式：源码静态检查、CLI/API 导入验证、单元/集成测试、演示数据结构检查。

执行结果：

- pytest -q：4 项失败，24 项通过。
- CLI 生成流程：在问卷清洗阶段失败。
- FastAPI 应用导入：因静态目录路径错误失败。
- 优化演示流程：CSV 被按 Excel 读取，失败。
- ruff check src tests：92 个问题。
- mypy src/cabin_eval：24 个错误。

## 2. 总体结论

项目已经具备较完整的目录分层，覆盖数据导入、用户分群、AHP/熵权、个性化、遗传优化、报告、API、CLI 和 Docker 等预期边界。但这些模块目前主要是独立原型，关键链路尚未闭合。

工程判断：未达到 design_one.md 的最小验收标准，当前处于 Phase 1 之前的原型骨架状态。

最直接的表现是：示例数据无法从 CLI 完成一次生成；Web API 在应用导入阶段无法启动；优化流程没有使用生成任务产出的模型；个性化和专家权威度等核心业务逻辑存在确定性失效。

## 3. 阻断级问题（P0）

### P0-1：问卷清洗函数必然报错

- 证据：[questionnaire.py:80](/home/robot/workspaces/smart-cabin/src/cabin_eval/ingestion/questionnaire.py:80)
- DataFrame.apply 默认按列传入 Series，回调中却调用 sum(axis=1)，触发 No axis named 1 for object type Series。
- CLI 生成流程和两个集成测试在分群之前就停止。
- 验收要求：清洗能够输出清洗结果、未知比例、剔除原因和填补记录。

### P0-2：FastAPI 应用无法导入

- 证据：[api/app.py:33-35](/home/robot/workspaces/smart-cabin/src/cabin_eval/api/app.py:33)
- BASE_DIR 计算结果是 src，实际模板和静态资源位于项目根目录的 templates/、static/。
- uvicorn 在应用创建阶段报 Directory '/.../src/static' does not exist。
- 验收要求：应用可导入，健康检查、首页和静态资源均可访问。

### P0-3：生成服务引用未导入的类

- 证据：[generation_service.py:166](/home/robot/workspaces/smart-cabin/src/cabin_eval/services/generation_service.py:166)
- 使用 IndicatorTreeManager，但文件顶部没有导入该类。
- 即使问卷清洗修复，流程仍会在计算权重前抛出 NameError。

### P0-4：演示模板和 README 指令不可执行

- 证据：[README.md:28-37](/home/robot/workspaces/smart-cabin/README.md:28)、[questionnaire_template.xlsx](/home/robot/workspaces/smart-cabin/data/templates/questionnaire_template.xlsx)、[indicators_template.xlsx](/home/robot/workspaces/smart-cabin/data/templates/indicators_template.xlsx)
- README 指向不存在的 questionnaire_demo.xlsx、indicators_demo.xlsx。
- 两个 .xlsx 模板实际是 CSV 文本，不是合法 XLSX 文件。

### P0-5：优化流程与演示数据格式不匹配

- 证据：[optimization_service.py:33](/home/robot/workspaces/smart-cabin/src/cabin_eval/services/optimization_service.py:33)、[optimization_samples_demo.csv](/home/robot/workspaces/smart-cabin/data/demo/optimization_samples_demo.csv)
- 优化服务固定使用 pd.read_excel，仓库演示文件是 CSV。
- 优化流程无法进入遗传算法阶段。

## 4. 核心算法和数据契约问题（P1）

### P1-1：问卷数据校验不符合设计契约

- 证据：[questionnaire.py:44-52](/home/robot/workspaces/smart-cabin/src/cabin_eval/ingestion/questionnaire.py:44)
- 对“不了解”等合法未知值仍进行数值比较，会触发字符串与整数比较异常。
- NaN 没有可靠纳入未知比例统计，空列可能一直保留为 NaN。
- 未校验人口属性枚举、ID 唯一性、评分列数、数值类型和 31 项功能完整性。
- 演示问卷的 function_24 到 function_31 全列为空，与 31 项有效需求约定不一致。

### P1-2：K-means 候选 K 没有受样本数约束

- 证据：[kmeans.py:23-39](/home/robot/workspaces/smart-cabin/src/cabin_eval/segmentation/kmeans.py:23)
- 默认候选范围到 10，没有限制 K 不得大于样本数。
- 小样本会在 KMeans.fit_predict 阶段失败。
- 自动选择没有实现肘部判断、跨随机种子稳定性和人工覆盖依据。

### P1-3：指标树没有构造隐含父节点

- 证据：[indicator_tree.py:46-60](/home/robot/workspaces/smart-cabin/src/cabin_eval/domain/indicator_tree.py:46)
- 构造器只在输入中已经存在父节点行时建立关系。
- 当前演示指标只有叶子行，因此 16 个指标全部成为根节点，安全、舒适、智能等层级关系丢失。
- 父子权重和、全局权重传播及优化约束因此无法按设计工作。

### P1-4：个性化权重实际没有生效

- 证据：[profile_adapter.py:37-77](/home/robot/workspaces/smart-cabin/src/cabin_eval/personalization/profile_adapter.py:37)
- 用户画像键是 function_1，适配器查找 function_IND001，需求等级几乎总是回退为 normal。
- 计算出的 final_local 没有写入复制后的树节点。
- 最终全局权重又从原始树读取，导出的个性化结果不可信。
- parent_weights 只累加，没有用于同父节点归一化。

### P1-5：专家权威度没有参与聚合

- 证据：[ahp.py:142-175](/home/robot/workspaces/smart-cabin/src/cabin_eval/weighting/ahp.py:142)
- 代码建立了 authority_map，但后续仍按专家结果简单平均，没有实现按权威度加权。
- 生成服务没有调用专家判断数据的 validate_judgments，结构错误可能直接进入 AHP。

### P1-6：熵权和约束投影存在已复现缺陷

- [entropy.py:38-48](/home/robot/workspaces/smart-cabin/src/cabin_eval/weighting/entropy.py:38) 对常数列除以 0，产生 NaN，对应单元测试失败。
- [constraints.py:21-44](/home/robot/workspaces/smart-cabin/src/cabin_eval/optimization/constraints.py:21) 先裁剪边界再归一化，归一化会再次突破 base ± epsilon，对应单元测试失败。

### P1-7：优化服务没有连接生成模型

- 证据：[optimization_service.py:23-57](/home/robot/workspaces/smart-cabin/src/cabin_eval/services/optimization_service.py:23)
- base_run_id 只被原样返回，没有读取对应生成任务的模型或基础权重。
- 初始权重始终使用等权向量，兄弟约束也被简化为所有指标一个组。
- 没有保存冻结权重、候选权重、新模型版本和完整切分依据。
- 空验证集会导致验证 MAE 为 NaN。

## 5. API、输出和审计问题（P1）

### P1-8：API 功能不完整且运行状态不持久

- 当前只实现生成、查询状态、下载产物和优化，缺少 profiles、weights、模型评分接口。
- [api/app.py:40](/home/robot/workspaces/smart-cabin/src/cabin_eval/api/app.py:40) 使用进程内 _runs，服务重启后运行记录丢失。
- 生成和优化在 async 路由中同步执行长任务，没有真正的任务队列。

### P1-9：上传和下载缺少安全边界

- 证据：[api/app.py:79-106](/home/robot/workspaces/smart-cabin/src/cabin_eval/api/app.py:79)
- 直接使用客户端文件名拼接路径，没有服务端文件名、路径穿越防护、扩展名/MIME/大小校验。
- [api/app.py:138-150](/home/robot/workspaces/smart-cabin/src/cabin_eval/api/app.py:138) 的下载接口直接拼接 name，没有产物白名单。

### P1-10：产物清单与实际文件不一致

- 生成服务声明 cleaning_report.json，但导出器实际生成 cleaning_report.xlsx。
- 图表虽生成，但没有完整进入运行产物清单。
- run_manifest.json 未包含代码版本、依赖版本、配置快照、输出哈希和警告闭环。
- [generation_service.py:179-185](/home/robot/workspaces/smart-cabin/src/cabin_eval/services/generation_service.py:179) 创建 EvaluationModel 时传入空 base_weights，且模型没有注册或持久化。

## 6. 测试和交付质量问题（P2）

- 黄金回归测试只有 pass，没有固定输入和期望输出：[tests/golden/__init__.py:5-8](/home/robot/workspaces/smart-cabin/tests/golden/__init__.py:5)。
- 缺少 API、服务层、完整生成、完整优化、并发隔离和错误输入测试。
- 缺少同输入、同随机种子的产物一致性测试。
- ruff 当前有 92 个问题，包含未导入符号、未使用变量、异步函数中的阻塞文件操作。
- pyproject.toml 只有 >= 依赖约束，没有锁文件；Docker 构建在复制源码前执行 pip install -e .，见 [Dockerfile:9-12](/home/robot/workspaces/smart-cabin/Dockerfile:9)。

## 7. 已具备的基础能力

- 目录分层覆盖了设计文档中的主要模块边界。
- Pydantic schema、YAML 配置、AHP 基础计算、K-means 指标计算、图表和基础 Excel/JSON 导出已有代码。
- CLI、FastAPI、Docker 和演示数据目录已经搭建。
- 部分单元测试能够验证 AHP、K-means、融合和 schema 的基本行为。

## 8. 建议修复顺序

1. 先修复 P0：问卷清洗、模板格式、API 根路径、IndicatorTreeManager 导入、CSV/XLSX 统一读取。
2. 补齐数据契约：评分方向、未知值、枚举、ID、31 项功能和指标层级的严格校验。
3. 修复核心正确性：父节点生成、个性化权重写回、AHP 权威度聚合、熵权常数列、带边界单纯形投影。
4. 让优化服务读取生成模型，建立真实的 train/validation/test、模型冻结和版本化产物。
5. 补齐 API 端点、运行记录持久化、上传安全校验、产物白名单和结构化错误响应。
6. 建立 30 条问卷、2 位专家、6 个指标、4 款车型的黄金集成测试，再处理 Docker、ruff、mypy 和依赖锁定。

## 9. 最小验收清单

- [ ] 使用仓库模板数据可完成一次生成。
- [ ] 使用同一生成任务可完成一次权重优化。
- [ ] 同输入、同配置、同随机种子得到一致结果。
- [ ] 每个指标父节点的子权重和误差小于 1e-8。
- [ ] 个性化体系与基础体系产生可解释的权重变化。
- [ ] AHP 不一致判断不会进入最终权重，专家权威度真实参与聚合。
- [ ] 优化后权重满足非负、和为 1 和 ±epsilon 约束。
- [ ] API、CLI 和服务层调用同一套业务逻辑。
- [ ] 运行记录、输入哈希、配置、代码/依赖版本和产物哈希可追溯。
- [ ] 单元、集成、回归、并发和错误输入测试通过。
